/**
 * Calculation
 * 
 * Mentioned in the UX Flow, but not given any obvious definition in
 * either the user journeys or the wireframes.
 * 
 * I'm going to assume it's something that takes inputs and produces an output, based on a function, but 
 * is it calculated somewhere else and we're just showing a result, or are we actually doing a calculation in the app? 
 */
export type Calculation = { 
  id: number;
}